version: 2
authtoken: ${NGROK_AUTHTOKEN}

tunnels:
  n8n:
    proto: http
    addr: n8n:5678
    inspect: true
    hostname: ${NGROK_DOMAIN}
    
  openwebui:
    proto: http
    addr: openwebui:8080
    inspect: true 