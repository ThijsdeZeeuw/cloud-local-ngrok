version: 2
authtoken: ${NGROK_AUTHTOKEN}

tunnels:
  n8n:
    proto: http
    addr: ${N8N_PORT}
    host_header: ${N8N_HOST}
    inspect: true
    # If you have a custom domain, uncomment and modify the following line:
    # hostname: ${NGROK_DOMAIN} 